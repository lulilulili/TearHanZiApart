# 矢量汉字笔画拆解：方案评估与落地说明

对应实现：`StrokeLab/prepareData.py`（数据预处理）+ `StrokeLab/charStrokeLab.html`（交互实验台，浏览器内运行完整拆解管线）。
前置背景：《与Gemini的前置分析.pdf》（骨架提取 → 非刚性配准 → 骨架引导切割 → 轮廓重构的四阶段纯矢量方案）。

## 一、对你 ①–⑤ 思路的评估与纠正

**总体判断：方向可行，且比前置分析里"从零做骨架提取"的方案更务实。** 但有四处需要纠正/加强：

### ① 从 U+31C0–31EF 或孤立笔画建标准笔画库 —— 成立，但覆盖是"看运气"
实测 Fonts 目录 9 个字体：
- HarmonyOS Sans SC / Noto Sans SC / Noto Serif SC：**36/48** 个笔画区字形；
- 马善政、站酷系列、WDXL、HarmonyOS TC：**0 个**，只能走孤立笔画提取，实测能拆出 **6–10 类**（横竖撇捺点、竖钩、斜钩、横折弯钩…）。

**纠正 1**：不能假设 B 库覆盖所有笔画类型。必须设计**回退链**：目标类型缺失 → 去钩简化（横折钩→横折）→ A 库（楷体形态）。实现中即如此。

**纠正 2**："孤立笔画"不能只找"整字即单笔"的字（一丨丶丿），要按**连通组粒度**判定：把探针字的轮廓按外轮廓+孔洞分组，采样点全部归属于同一条楷体中轴线、且该笔画只对应这一个组时，即为孤立笔画。

**建库规则表（v2）**：A/B 库统一按"32 类笔画 × 独立/少相交代表字"规则表建库（横←一二三、竖←川旧、撇←八儿川、点←太犬、捺←八父又大人、…、卧钩←心必志思念，全表见 charStrokeLab.html 中 `PROBE_TABLE`）。提取出的笔画**类型直接取自规则表**，几何分类器只负责在代表字内确认"该孤立组对应的楷体笔画确属此类型"（匹配分两档：完全一致 / 骨架一致——骨架化=首元素+中间一律视作折+钩后缀）。这消除了此前"由分类器给任意笔画命名"造成的 B 库类型错误。

U+31C0–31E5 笔画区按 **Unicode 官方命名**收录（`CJK_STROKE_NAMES`/`CJK_STROKE_ABBR`，中文名按官方缩写还原：㇀=提/T、㇆=横折钩/HZG、㇇=横撇/HP、㇛=撇点/PD、㇝=提捺/TN、㇣=圈/Q、㇤=横斜钩/HXG、㇥=竖折撇/SZP 等），B 库卡片来源栏显示"U+编码 字形 缩写"。官方名与本项目分类器词汇（横撇、横折钩、竖弯钩、撇点、撇折…）高度一致，Unicode 条目可直接精确命中同类查询。

**尺度不变形状描述子（v3）**：回答"不同尺寸/形态下如何判定同一种笔画"——对任意闭合路径计算平移/缩放不变、方向敏感的特征向量：①切向直方图（轮廓按弧长加权的方向分布，mod 180° 共9桶）②长度加权 PCA 主轴角+伸长率 ③充实率（|面积|/包围盒面积）。实测区分度：横↔竖距离2.7、撇↔捺2.7、横↔横折3.3；同类跨来源（Unicode 横 vs 从"一"拆取的横）仅0.03。三处应用：
- **B库建库校验**：拆取候选与该类型 Unicode 权威笔画（或楷体该笔）比对，形似<20% 剔除，卡片显示"形似 xx%"；
- **S2 模板选择兜底**：类型名查不到时按描述子跨类型找最形似的库条目（纯缩放无关匹配）；
- **D′ 自检**：⑥表新增"形状匹配"列（D′ vs 楷体同笔），楷体与目标风格差异下 40–80% 为正常区间，<35% 提示该笔切分可疑。
拆解管线本身的"缩放匹配"由三层机制承担——全局仿射（字面级）、逐笔宽度归一化评分（笔宽级）、直笔沿主方向 1D 伸缩+折笔分段平移（笔长级）；描述子负责"类型判定与结果校验"而非切割定位。

### ②③ 建 A/B 库 + 取 makemeahanzi 结构 C —— 成立，且比你预想的更省
makemeahanzi 的 `graphics.txt` 除逐笔轮廓外**自带逐笔中轴线（medians）**，`dictionary.txt` 自带 `matches`（笔画→部件归属）。这意味着：
- 前置分析中"阶段一：CDT 骨架提取"对**基准侧完全不需要做**；
- 目标侧也可以不做显式骨架提取（见下）。

**纠正 3**：makemeahanzi 不含笔画类型标注，A/B 库的"类型"键需要自建分类器。实现用"重采样 → 拐角分段（窗口化切向差）→ 每段净方向 → 折/钩组合命名"，需注意楷体**起笔/收笔顿笔**会造成伪分段（须丢弃首段短段），钩的判定要区分真提（竖提）与收钩。分类结果 A、B 两侧用同一分类器保证键一致。

### ④ 用 ABC 合成 D —— 定位要改：D 是"先验/校验模板"，不是拆解结果
把 B 库笔画按中轴线框放置得到的 D，只能预测"这笔在目标字体里大概长什么样、多宽"。**拆解不能以 D 为准去裁剪**，因为字库里的笔画有非刚性形变（长短、倾角、连笔），模板贴不准。D 的正确用途：宽度先验、类型校验、以及最终质量比对基准。

### ⑤ 提取 D 特征去实际路径里映射拆解出 D′ —— 核心正确，实现路径要具体化

**（v5 流程修正）**匹配模板必须是 D 而不是楷体中轴线本身：早期实现曾直接拿楷体 medians 与目标字形匹配（用户指出此偏差后已修正）。现行流程：**A↔B 按类型映射**——把 A库（楷体）同类型中轴线 bbox 映射进 B库笔画轮廓并做单笔精调拟合，得到**目标字体自己的笔画形态骨架**；再按楷体结构 C 给出的该笔包围盒定位，构成 D；后续归属/切割全部以 D 骨架为模板。B库缺该类型时才退回楷体中轴线。楷体只提供笔顺/类型/相对位置，形态来自目标字体本身。
这一步就是前置分析的"配准+切割+重构"，实现为纯矢量五步（页面 S1–S5 可视化）：
1. **全局仿射对齐**：楷体笔画外轮廓包围盒 → 目标字形包围盒（注意不能用中轴线包围盒，会差半个笔宽）。
2. **迭代精调（3 轮）**：逐笔估计笔宽（归属样本距离中位数×2）+ 垂向平移 + 直笔画沿主方向 1D 伸缩。两个关键坑：
   - **笔宽正反馈**：评分若用各笔自身宽度归一化，会"越宽越抢"——须对评分用宽度做全局中位数钳制（0.55–1.6×）；
   - **楷体中轴线偏短**：楷体笔画端点常比目标字体短一截，评分时须把中轴线两端沿切向虚拟延长（≈1 倍笔宽），否则长捺的末端会被邻笔抢走。
3. **边界归属**：轮廓按曲率密度采样（参数点，非像素），评分 = 宽度归一化距离 + 0.5×切向一致性罚项，argmin 得标签；短游程平滑。
4. **矢量切割**：标签跳变处沿轮廓参数二分 22 次，De Casteljau 精确切段。**保留区段是原字形贝塞尔段的精确切片**——满足"拆出笔画与原路径形态一致"的硬要求。
5. **闭合重构**：每笔弧段端点就近串联，交叠区缺失边界用 C¹ 三次贝塞尔桥接（手柄长 ≈ min(0.45×间距, 0.7×笔宽)），输出独立闭合矢量路径 D′。

## 二、与前置分析（Gemini 方案）的关系

| 前置分析阶段 | 本实现 | 说明 |
|---|---|---|
| 阶段一 CDT 骨架提取 | **跳过** | 基准骨架直接用 makemeahanzi medians；目标侧用"边界点→中轴线归属"替代显式骨架 |
| 阶段二 TPS/CPD 非刚性配准 | **降级为** 仿射 + 逐笔平移/伸缩迭代 | 对常规字体足够；连笔严重的手写体是主要误差源 |
| 阶段三 凹角点检测+割线 | **替代为** 标签跳变的参数二分 | 避开了"圆角无凹点""T型搭接单凹点"两类失灵场景 |
| 阶段四 轮廓重构 | 同思路（C¹ 桥接 + 环路串联） | 交叠区当前为硬分割，未做"双重归属"（见局限 3） |

## 三、实测结果与"并集恒等"要求（重要）

**硬性要求（用户定义）：所有笔画融合（并集）后必须与原始路径 100% 一致——不多不少。**（笔画之间允许交叠=双重归属，不影响并集恒等。）

为此重构了闭合重构架构（v4，划分式）：
1. **桥接一律用直割线弦**（两个切割点之间的直线段）：曲线桥/切线延伸桥会外凸或探出字形，已弃用；弦位于字形内部，各笔的弦把原区域切成划分，交叠区被相邻笔画同时包含。
2. **环路追踪**：同一条原始轮廓上属于本笔的弧段按轮廓参数顺序首尾相连（此前"端点就近串联"会把融合字形中笔画的两条长边闭成零面积退化环，导致大面积漏填——手写体"永"的中柱曾因此全白）。
3. **跨轮廓并环**：环形笔画（口/国的横折等）在外轮廓与孔洞上各有一条链，用两条径向弦并成环形段。
4. **孔洞标签径向对应**：内边归属直接取正对外边的标签，保证内外一致。
5. **重并集校验（Re-Union）常驻⑥面板**：网格抽样计算"原形覆盖率"（应100%）与"溢出率"（应0%），不达标红字警示。

**当前实测**（9字体×17字）：平均覆盖 96.5%、平均溢出 6.1%；标准印刷字体（HarmonyOS/Noto系）大部分字已达 100%/0%，严格达标（覆≥99%且溢≤1%）约 1/3。**尚未全面达标**，三个已定位的残余来源：
- 环形并环的方向性：艺术字体"国"的孔洞链起点落在对角时并环退化为对角弦（溢出40-55%）；
- 连笔字体的标签碎片（如小魏"水"、Noto"爱"）：弧段碎片被长弦硬连或漏连；
- 凹形交汇处直弦轻微出界。

**收口方案（下一步）**：矢量布尔裁剪+残差回填——每笔环与原字形求交（保证"不多"），未覆盖面片按邻接笔画回填（保证"不少"）。这是保证性的最后一道闸，几何拼装负责"尽量对"，布尔闸负责"绝对对"。

**"永"案例暴露并修复的四个问题**（楷体与现代字体结构比例差异大的典型字）：
1. **重叠≠嵌套**：未做布尔并集的字体（如 HarmonyOS）中两笔轮廓部分重叠，单点包含测试会把重叠误判为"孔洞"→ 包含测试改为 8 采样点占比 ≥80% 才算嵌套；
2. **折笔精调无力**：横折钩的横段/竖段在楷体与目标字体中的相对位置差异大，整体平移修不动 → 精调升级为按拐角分段、逐段独立平移（拐角顶点取两侧均值过渡）；
3. **精调失稳**：分段平移可能被少量误标样本逐轮拖走（"汉"的横撇曾被拖到氵的点上）→ 段偏移改用分量中位数 + 顶点相对初始仿射位置的累计位移硬上限 max(80, 1.3×笔宽)；
4. **孤立/叠压轮廓被抢边**：氵的点这类孤立轮廓、以及未做布尔并集字体里互相叠压的完整笔画轮廓（黑体"永"的捺压着竖），边缘采样点会被邻笔评分抢走造成切碎/缺口 → **轮廓"主人判定"**：按各笔中轴线落在轮廓内的占比（精调后与初始位置分别计算），唯一主人（max≥0.55 且 min≥0.35）且无第二竞争者（max≥0.45 即视为融合字形共有、跳过）时，整条轮廓判给主人；附饿死保护。修复后黑体"永"五笔保留率 98–100%、"十"双笔 100%。
（曾尝试按楷体周长份额做"容量平衡"惩罚，因现代字体点画占比天然大于楷体、先验失真误伤而弃用。）

## 四、已知局限与下一步

1. **交叠区硬分割**：当前切割点两侧非此即彼，笔画在交叉处是"平口"而非穿越交叠区的完整形态。改进：对每个切口按对侧端口配对做双向 C¹ 插值（前置分析"Multiplexing & Inpainting"），让交叠区几何同时归属两笔。
2. **配准仅平移+单轴伸缩**：折笔（横折钩等）只平移。改进：对折笔按拐角分段各自精调，或上 TPS。
3. **无自动质量校验**：建议加 Re-Union IoU（拆出笔画布尔并集 vs 原字形，矢量扫描线即可），IoU<99% 触发回退。
4. **笔画类型分类器约 90% 准确**（好=竖捺? 中=横捺撇?）：仅影响 A/B 库取模板与显示标签，不影响切割几何。可用固定字表校准或人工修表。
5. **连笔字体**（拓扑异构，笔画数不一致）未做"虚拟节点分裂/图重写"，是最难场景。
6. **连笔字体**（拓扑异构，笔画数不一致）未做"虚拟节点分裂/图重写"，是最难场景。

## 五、使用方式与文件清单

**双击 `D:\07_chcharapart\StrokeLab.bat` 即可使用**（需已安装 Python 3，仅用来起本地静态服务）：它会自动扫描 Fonts 目录生成字体清单、启动 localhost:8632 并打开页面；关闭黑窗口即停止。

页面为**全运行时架构**，无需任何预处理：
- 启动时直接加载 makemeahanzi 原始数据（graphics.txt 约 30MB，浏览器有缓存，仅首次稍慢）与 hanzi_chaizi 拆字表；
- 字体用本地打包的 opentype.js 在浏览器内解析（TTF/OTF/可变字体均可），标准笔画库 B 现场构建；
- **任意汉字**（makemeahanzi 覆盖约 9500 字）在输入框回车即拆，复杂字如"爨"30 笔约 200ms；
- 支持**拖入/选择任意 ttf·otf 文件**作为目标字体，即时建库拆解。

文件：
- `StrokeLab.bat`：一键启动（生成 fontList.js → 起服务 → 开浏览器）。
- `StrokeLab/charStrokeLab.html`：六面板实验台——①真实子路径着色 ②楷体笔顺动画 ③A/B 库(含来源) ④多层级结构 ⑤S1–S5 流程可视化 ⑥D′ 按楷体笔顺播放。数据加载、笔画分类、A/B 建库、拆解管线全部在浏览器内纯矢量运行。
- `StrokeLab/opentype.min.js`：本地打包的字体解析库；`StrokeLab/fontList.js`：bat 自动生成的字体清单。
- `StrokeLab/prepareData.py`：（可选，遗留）离线预处理版数据管线，算法与页面内 JS 一致，可用于批量导出。

## 六、v6：Python 迁移与"并集恒等"达成

按用户决定，算法从浏览器 JS 迁移为 **Python 包 `strokelab`（唯一算法源）**，HTML 降级为纯可视化前端（六面板与交互不变，算法走本地服务 API）。迁移动机与红利：

1. **shapely 布尔收口落地**（`strokelab/boolean.py`）：每笔与原字形求交（保证"不多"）+ 未覆盖残差面片按共享边界回填（保证"不少"）；仅对确有问题的笔画替换为裁剪路径，其余保留贝塞尔精确切片。**实测 9 字体 × 17 字 149 组合全部严格达标：平均覆盖 99.97%、平均溢出 0.00%、strict100 = 100%**——"所有笔画并集与原字形恒等"的硬性要求达成。
2. 确定性与可推广：pip 包 + CLI（`python -m strokelab.cli`）+ 服务 API（`python -m strokelab.server`），149 组合指标可固化为回归金标准。
3. 顺带修复：TrueType 多控制点二次曲线展开的隐性偏差（legacy/prepareData.py 时代带入）。

结果表新增"路径来源"列：贝塞尔精确 / 布尔裁剪，可区分每笔是否经过收口修正。
