# 矩阵归并设计 v1（评审#4"统一代价矩阵"分步落地·第一步）

> 依据：docs/架构评审与优化路线.md 第4项裁定——**认终态，分步走**。算法事实：
> G3部件/G8楷序是 pairwise、G4/G5 是容量约束，unary 矩阵表达不了（硬塞=二次
> 指派 NP-hard）。正确形态 = **unary 证据并矩阵 + 少量拓扑仲裁 + 几何吸附**
> 三阶段；每归并一级过抽样/bench/Codex 门，不做大爆炸。
> 数据依据：verifyOut/traceStats/20260913-203447-sample/traceStats.md
> （rev 0177652，sample 958，鸿蒙+simsun 双字体胜率表）。

## 终态三阶段与现有十二级的映射（定位用，非本轮范围）

| 终态阶段 | 现有级 | 归并可行性 |
|---|---|---|
| unary 证据并矩阵 | penMatrix(杆⊥/点锚)、G5杆容量、G2走廊支撑 | 笔×组标量，可并（本轮对象） |
| 拓扑仲裁保留 | G3部件、G4/G4v、G6全排列、G7槽位、G8.5梯队秩 | pairwise/容量/排列，矩阵表达不了，**不动** |
| 几何吸附 | G9重锚定、G10断面吸附 | 位置修正非指派，不在归并射程 |

## 1. 胜率表摘要与归并选型（数据依据）

| 级 | 鸿蒙(957测) | simsun(957测) | 裁定 |
|---|---|---|---|
| G8 | 触发23字/54笔次，采纳率 **3.7%**，采纳字通过率 **0.0%**（攮） | 触发93字/256笔次，采纳率 **0.8%**，采纳字通过率 **0.0%**（銲） | **行动层判死刑**：双字体合计仅4笔次施行且采纳字全败。但触发字通过率 78.3%/74.2% 远低于未触发 96.7%/89.4%——楷序反转判据有**病征价值**，降级为诊断信号 |
| G5 | 触发386字（**40.3%**）/499笔次，采纳率 **2.9%** | 触发62字/77笔次，采纳率 10.0% | **空转**：病征门(笔比杆长>1.08×)触发面大、三步链(认领孤儿+回填+楷序落点一致)几乎不成立。其核心证据"杆容量"是 chord/barLen 的笔×组 **unary 量**——折进 penMatrix，层保留兜漏网 |
| G2 | 触发290字/399笔次，采纳率 **14.8%**，采纳字通过率 87.9% | 触发387字/575笔次，采纳率 **12.7%**，采纳字通过率 74.6% | 在干活且证据 corridorSupport(k,g) 天然是笔×组 unary 量——可折，但排 G5 之后（见 2c 方案对比） |
| G10 | 触发550字，采纳71.3% | 触发 **909字（95.0%）**，采纳 **33.4%** | **阈值无量纲化问题**：走廊支撑贫瘠判据(<标称35%)与绝对面积阈值(400/800/900)未按笔宽 wEst/EM 归一，宋体细横走廊面积天然小→接近全量误触发。**记入评审#2 Config 无量纲化审计清单，本轮不动 G10** |

选型原则：只归并"证据本身是笔×组标量"且行动层实证低效/零效的级；
G1(100%采纳,锚定恒施行)、G4(采纳67.8%)、G6/G7(低触发防护层)、G8.5(采纳85.7%,
pairwise)、G9(97.3%)均不动。

## 2. 第一步归并方案（本轮落地范围，保守）

### 红线（先于一切方案）

- **罚只进匈牙利锚定矩阵，不进 costRows 本体**——罚进本体曾把爱5竖的
  G3 回家路堵死（assign.py 注释在案）。argmin 就近入组、G2..G10 全部
  代价窗、G9/G10 起点仍用纯几何代价。
- **每子步独立开关、独立过门、独立可回退**（包属性，赋值即生效，照
  TRACE_ON/LADDER_PROBE/CLIB_ENABLE 语义）：a=`ARB_G8_EXEC`（默认
  True，撤除即置 False）、b=`PEN_BARCAP`、c=`PEN_CORRIDOR`（默认
  False，达门才转默认开）。任一子步翻转清单不干净即单独回退，不牵连
  其余。
- **G3部件/G8.5梯队等 pairwise 级不动**——评审明示矩阵表达不了；
  G8.5 刚落地 0 回退（docs/梯队秩配对设计.md），不叠加变更。

### 2a. G8 降级：撤互换执行，判据保留为诊断信号

- 改动面：arbitrate.py `orderPreserve` 采纳分支不再改
  strokeGroup/groupStrokes；判据评估与 trace 全保留，原 adopted=True
  条目改记 `action="demoted", adopted=False`（evidence 含 wouldSwap
  明细），胜率表可继续统计病征命中。
- 病征价值保留理由：触发即"高危字"标记（通过率 78.3%/74.2% vs 未触发
  96.7%/89.4%），供失败分诊、重试链与未来 pairwise 层选字用——判据
  没错，错的是"互换"这个动作（同型对楷序反转多为字体比例设计差异，
  与 verify 把跨型方位翻转降软指标同一认识）。
- 预期：**零回退**。行为变化仅限双字体各2采纳笔次（鸿蒙攮、simsun銲，
  且现状均失败）；其余触发字触发≠采纳、无副作用，撤除后应逐位同判。

### 2b. G5"杆容量"证据折进 penMatrix

- 判据（与 G5 门同源）：定向笔(横/竖/提，名义弦长≥40，复用现有 kAng0
  门) × 杆状组(复用 penMatrix 段已建 `_barAx` 杆表：单外环无孔 +
  aspect≥3.0 或 elong≥2.5) 且**近平行**(轴向折叠差≤30°，与杆⊥罚
  >50° 互斥不叠加)，杆沿笔轴向长度 `barLen = |bb.w·ux|+|bb.h·uy|`
  （与 G5 `barTheftSwap` 同投影公式），`chord > 1.08 × barLen` →
  该(笔,组)对 `pen += 200`（标定区间 120~250）。
- 罚幅推导：G5 场景=名义压杆代价≈0、真主组(孤儿墨侧)代价可达 140
  （G5 认领窗上限），罚须覆盖该差值才能在锚定层翻转 → 起点取 200，
  与杆⊥罚同档。
- 作用机制（诚实版，非"完全替代三步链"）：罚只改**谁锚定杆**。过长笔
  被罚出锚定后经自由列以 argmin 仍可能就近回到杆组，但杆已由匈牙利
  锚给装得下的同向笔——"过长笔单独直出杆"的病征在锚定层消解为多成员
  竞争组，交采样迭代/切割分墨。G5 触发条件(单笔独占组)因此骤降；
  **G5 层保留**处理漏网（孤儿墨认领链仍有 28/14 笔次真实修复量）。
- 已知噪声面：G5 触发 40% 采纳 2.9% 说明 1.08× 单证据偏弱（字体把杆
  画短是常态）。对策：罚是有限量非否决——健康字的杆若确是该笔唯一低
  代价家，+200 罚不足以把锚定推给荒谬替代（匈牙利仍选它）；标定时若
  健康字锚定漂移超预期，先提门槛(1.08→1.15/1.25)再降罚幅。

### 2c. G2 走廊支撑折进代价面：方案对比与推荐

| | 方案A：折进 G0 costRows 本体 | 方案B：折进 penMatrix | 方案C：本轮不折 |
|---|---|---|---|
| 形式 | cost' = 墨距离 − f(支撑) | 窗口内支撑比值贫瘠罚 | 维持 G2 事后仲裁 |
| 量纲 | **不通**：支撑是面积(∝笔长×wEst)，墨距离是长度；线性混合=字体笔宽敏感的 magic 系数，正是评审#2要清的账 | 比值 `sup(k,g)/max_h sup(k,h)` **无量纲**；罚幅独立标定 | 无新量纲 |
| 符号 | 奖励项(负修正)会击穿自由列语义——自由列代价=min(纯 costRows)，负奖把真实列人为压到自由列之下，制造虚假锚定偏好 | **只罚不奖**(正号)，与现 penMatrix 结构一致；罚支撑贫瘠组=等价奖励支撑强者，自由列逃生口不受损 | — |
| 红线 | **违背**：本体修正传导到就近入组与 G2..G10 全部窗口（爱5竖同型事故） | 合规：只影响锚定 | 合规 |
| 幅度 | — | 比值<0.5 且存在窗口内(≤max(35, cost×0.6)，与 G2 同窗)更优候选 → `pen += 100`（区间 60~150；走廊是软证据，须低于杆⊥/点锚铁证档 200/250） | — |
| 成本 | 全(笔,组)对算 shapely 支撑，assign 阶段 O(n²)×13次平移求交，不可接受 | 只对"代价窗口内有他组候选"的笔算(与 G2 门同)，且需把 regionOf 缓存从 corridorFit 前移到 assign（挂载顺序改动） | 零 |
| 收益 | — | 匈牙利全局看到容纳度；G2 采纳的 59/73 笔次改判有望前移到锚定层 | G2 采纳率 14.8% 且采纳字通过率 87.9%，现位并不病 |

**推荐：B 形式，但作为第一步的第三拍缓落**——先落 2a+2b 过门，2b 的
翻转清单干净、复跑胜率表确认 G5 触发降幅后，再动 2c（B 的比值分母
`max_h sup` 依赖候选集，与 2b 改锚定有交互，同批落地无法归因）。2c 落地
时 G2 层同样保留（罚后仍漏的窗口错分由它兜），达门后视残余触发量评估
第二步是否撤层。

## 3. 验收方案（每子步独立过门）

| 门 | 标准 |
|---|---|
| bench | 0 破坏（verified 门） |
| sample 958 | ≥ **96.24%**（当前基线，不设容差） |
| cross 三字体 | 不低于基线：simhei **88.71** / Noto **74.06** / simsun **87.15** |
| 翻转清单 | tools/diff_runs.py，回退>0 即退出码非零、逐字裁定；批跑走 docs/批量验证运行器.md 流程 |
| 胜率表复跑 | 同字集同口径重跑 traceStats，与 20260913-203447 对比 |

预期指标（复跑胜率表量化）：

- **2a（G8撤除）**：零回退硬指标——未采纳触发字（鸿蒙23字/simsun93字
  减去采纳字）diff_runs 断言 0 翻转（撤除只动施行分支，理论逐位同判，
  翻不动即实现有误）；行为变化字仅攮/銲，逐字目检（现状已失败，
  不劣化即过，修复算白赚）。
- **2b（杆容量罚）**：G5 触发字数降幅 ≥50%（鸿蒙 386→≤190 为达标线，
  实测为准）；G5 残余采纳笔次不归零（层仍在干活的证据）；G1 迹中
  pen 命中的字清单全审（evidence.pen 字段现成）。
- **2c（走廊罚，缓落）**：G2 触发/采纳双降；G2 原 59/73 采纳笔次所在
  字零回退。

## 4. 风险清单

| # | 风险 | 缓解 |
|---|---|---|
| 1 | **penMatrix 加罚改锚定→连锁改下游起点**：G1 锚定结果变化 → G2/G3 "匈牙利锚定不碰"(strokeGroup≠argmin 跳过)的保护面移动、就近入组格局变化。代码事实核对：S1b 只在组数>笔数触发而匈牙利只在组数≤笔数运行，S1b 空组集合理论上不受 penMatrix 影响——但翻转清单仍须覆盖 S1b 路径字回归 | 罚幅从保守值起标定；每拍独立开关+翻转清单全审；G1 迹 pen 字段定位每一处锚定漂移 |
| 2 | **新罚与 G8.5 执行器的 penMatrix 否决门冲突**：被逐者重指派现读 `penMatrix.get(o,{}).get(g,0) >= 200` 一票否决，且卑族教训在案（杆⊥罚曾否决正当结构互换，已为 D/C 型设豁免）。容量罚取 200 档会进入该否决面 | 首版接受（容量=硬容纳证据，与杆⊥同性质）；若 G8.5 家族25字回归出现翻转，改罚种分道记账（penMatrix 条目带 tag，否决门只读杆⊥分量），并沿用 D/C 豁免语义 |
| 3 | **G5 门 1.08× 单证据噪声**（触发40%采纳2.9%的另一面）：健康字的"字体短杆"被罚，锚定推给次优组 | 罚有限非否决+匈牙利全局权衡兜底；标定顺序=先提门槛后降罚幅；cross 三字体门拦字体特异性回退 |
| 4 | **G8 撤除影响的 23（鸿蒙）/93（simsun）触发字逐字体检计划**：①撤除前后全字集 diff_runs，断言未采纳触发字 0 翻转（应逐位同判）；②采纳字（攮/銲）viewer 逐字目检切割终态；③cross 第三字体 simhei 补跑 G8 触发字子集（本轮胜率表无 simhei 数据，触发面未知）；④trace 复跑确认 demoted 条目证据完整（病征统计不断档） | 计划即缓解；任一步异常回退 `ARB_G8_EXEC=True` |
| 5 | **2c 的 regionOf 缓存前移**：走廊支撑现依赖 corridorFit 挂载的 groups.regionOf/pose.wEst，折进 assign 需前移构建时机，改动挂载顺序 | 归入 2c 拍单独落地；缓存本体 groupRegionOf 已是模块级函数（partial 绑定），前移只动接线不动几何 |
| 6 | **simsun G10 95% 触发遗留**：本轮不动，但它提示全部走廊/面积类绝对阈值(400/800/900/35%标称)对细笔画字体系统性失真，2b/2c 标定若在 simsun 上异常，优先怀疑同源问题 | 记入评审#2 Config 无量纲化审计清单首位；2c 已按比值无量纲设计规避 |

## 实施顺序

1. 2a G8 降级（`ARB_G8_EXEC`）→ 过门+风险4体检 → 提交；
2. 2b 杆容量罚（`PEN_BARCAP`）→ 标定(门槛/罚幅) → 过门+胜率表复跑
   （G5 降幅指标）→ 提交；
3. 2c 走廊贫瘠罚（`PEN_CORRIDOR`，含 regionOf 前移）→ 过门 → 提交；
4. 移交单给 Codex：对抗审查点=G8.5 家族25字回归（风险2）、G5 原28采纳
   笔次字清单复核（折罚后修复不丢）、simsun 细笔画字体标定数据；
5. 第二步（另立文档）：视残余触发量评估 G5/G2 撤层、评审#3 dummy node
   矩阵重构形态、#2 无量纲化审计（G10 阈值在此收拾）。

## 对抗评审裁定(2026-09-13,三镜头,verdict: approve-with-fixes)

### 致命伤与修订(约束性,实施必须遵守)

1. **审计仪器先行(2b 硬前置)**:现 G1 迹的 evidence.pen 实证为空
   (1207 条中 0 条命中——罚把笔推离被罚组,成功时目的组必然无罚,
   而埋点只记目的组罚)。修订:①trace 记 penFrom+penTo,penMatrix
   按罚种分道记账({'barPerp':..,'barcap':..});②TRACE_ON 时用
   无新罚矩阵再解一次匈牙利,两解不同的笔记 penShift 条目——这才是
   罚作用面的真实清单(冒烟:现有杆⊥罚在 958 上须产非空 penShift)。
2. **新工具 tools/diff_traces.py(隐性漂移仪)**:diff_runs 对"锚定变
   但终态同"全盲,而 2b 的主要作用形态正是这种漂移(漂移还收缩
   G2/G3 对该笔的保护面)。逐字比对迹条目多重集,漂移字数入验收。
3. **2b 判据面对齐 G5 实码**:实测罚(带杆形门)对 G5 触发对命中率仅
   44-46%,降幅上界约 45%——撤掉 _barAx 形状门与笔型门,只保 1.08×
   投影判据;降幅指标 ≥50% 改 **≥35%**。
4. **G5 保留层门扩展**:罚造出的双成员杆使"单笔独占"门失效,21 个
   G5 采纳字模拟中 ~7 个现通过字(减儋擔檐鹈×2舲)的已验证修复链
   被打断。修订:G5 门扩为"单笔独占 或 组内存在 barcap 命中对";
   **硬门:21 个 G5 采纳字逐字零回退,否则 PEN_BARCAP 单独回退**。
5. **2a 全库专项集**:sample958 按构造测不到全库 ~20 个 G8 采纳字
   (stride 抽样错位)。落地后全库枚举 demoted 字集做专项 diff;
   demote 不置 swapped(单 pass 口径,胜率表口径注明);銲 撤除前后
   截图归档为 2a 出口物。
6. **[3,4) 死区**:罚杆形门(若保留)与 G4 _isBarGroup 阈值对齐,
   否则两根平行笔挤死细杆无任何层可解。
7. **G8.5 联动**:回归集扩为 家族25 ∪ {儋擔戆缴銲}(G5→G8.5 采纳链
   实证依赖);barcap 罚在 G8.5 执行器 ≥200 否决门须分道豁免。
8. **基线重钉前置**:G8.5-F 落地后先复跑 traceStats+sample 重钉基线,
   再启动 2a——跨工作流混改态下 diff 无法归因。

### 宝贵负结果(评审实测,落地时可直接引用)

- G8 隐性依赖不存在:23+93 触发字逐字插桩含种子重跑遍,G8 变异 0 例
  ——"未采纳触发字 0 翻转"断言前提在两字体成立,2a 安全性有实证。
- 銲 的 G8 采纳与 G5 链耦合(重排 G5 刚搬的两笔),2a 出口物含其目检
  归档,作为 2b 启动前置。

### 修订后实施顺序

0. 等 G8.5-F 提交 → 复跑 traceStats/sample 重钉基线;
1. 审计仪器(penFrom/penTo/penShift + diff_traces.py)+ 冒烟;
2. 2a G8 降级(全库 demoted 专项集 + 銲归档);
3. 2b barcap 罚(对齐判据面 + G5 门扩展 + 21 字硬门 + 分道豁免);
4. 复跑胜率表评估 2c(走廊)是否进入下一拍。
